# チェック機構の型

自動はスクリプト一本だけ。Claudeを走らせる自動チェックは置かない。

## 自動（トークンを使わない）

`scripts/check.js` を用意し、`src/` を変えたら走らせる。
中身は機械で確かめられるものだけ。

- つまみの一覧と、実際に画面から動かせる数が一致するか
- 定数がファイルの先頭にまとまっているか
- 触るなと決めた箇所が変わっていないか
- 仕様書の版と、実装が参照した版が一致するか

## 手動（合図で呼ぶ）

- **「監査して」** ＝ spec.md だけを読み、曖昧・矛盾・未決を指摘する。実装しない。
  spec.md を大きく書き換えた直後に一回
- **「検算して」** ＝ spec.md と実装を突き合わせる。実装したセッションとは別で。
  人間が触って判定する直前に必ず一回

## 自己提案

CLAUDE.md の一行（仕様書に無いことを三つ以上決めることになったら止める）だけ。
条件を増やさない。増やすと効かなくなる。

## 一回の山につき

監査1回・検算1回。それ以外はスクリプトだけ。
